npm install
npm run dev


The application provides a Create Lead UI, so no separate seed script is required. After logging in, navigate to the Lead list page with you can see the add button so the first lead you can create.

Tech Stack :-
React.js
Vite
React Router
Tailwind CSS
Lucide React